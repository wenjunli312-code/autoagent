# 车载 Agent：为什么是必要架构，而不是可选项

## 核心洞见：纯 LLM 无法落地车载

LLM 的推理弱点在学术界已被充分验证——**单步推理准确率约 90%**。单轮问答看起来还行，但放到车载场景：

```
用户指令 → 理解意图 → 场景判断 → 规划路径 → 调用执行 → 结果验证
  ↓          ↓          ↓          ↓          ↓          ↓
 90%  ×     90%  ×     90%  ×     90%  ×     90%  ×     90%  ≈ 53% 成功率
```

**六步推理链仅有约 53% 的端到端成功率。** 
这在智驾上不可接受。每一次失败都意味着功能不可用、用户困惑、甚至安全隐患。

## Agent 解决了什么

Agent（LLM + Tool Use + ReAct 循环 + 记忆 + 错误恢复）改变了这个等式：

```
LLM 单步推理         执行失败？→ 重新观察 → 重新规划 → 再次尝试
     ↓                           ↻
  90% 准确率              Agent 循环内自动兜底
```

- **单次失败 ≠ 任务失败**：Agent 会观察结果、分析原因、换方案重试
- **每步都有验证**：Tool 返回结果后，Agent 判断是否合理，不合理就回退
- **端到端成功率从 ~53% 提升到 95%+**（学术界已验证，ReAct / Reflexion 等范式）

这不是「跑得更快」的问题，而是 **「从不可用到可用」的质变**。

## 车载场景为什么更需要 Agent

通用场景的 Agent 已经很强，但车载场景放大了 Agent 的必要性：

| 纯 LLM 方案 | LLM + Agent |
|------------|-------------|
| 一步到位，错了就错了 | 多步推理，可纠错可回退 |
| 无工具调用，纯文本输出 | 调用传感器、规划器、执行器 |
| 无状态，每次都是新的 | 有记忆，记得刚才发生了什么 |
| 无法应对异常 | 感知异常 → 分析原因 → 换方案 |
| 无法解释决策 | 每一步推理可追溯、可审计 |

在车上，任务的链条更长、风险更高、用户预期更强。纯 LLM 无法应对。

## 技术本质：Agent = 推理层 + 工具层 + 安全层

```
用户输入（语音/触控）
      │
      ▼
┌─────────────────────┐
│   Agent 推理层       │  ← LLM 为核心，理解意图，制定计划
│  (Think → Act → Observe)
└──────┬──────────────┘
       │ 调用
       ▼
┌─────────────────────┐
│   工具层              │  ← 调用车载能力：感知/规划/控制/座舱
│  (Tool Execution)    │
└──────┬──────────────┘
       │ 结果回传
       ▼
┌─────────────────────┐
│   安全层              │  ← 规则引擎交叉验证，拦截不安全决策
│  (Safety Guard)      │
└─────────────────────┘
```

三层解耦：
- **推理层**负责智能——可以持续升级
- **工具层**负责执行——复用已有模块
- **安全层**负责兜底——确保任何情况下不输出危险指令

**这是量产车载 AI 不可跳过的架构。** 不做 Agent，LLM 在车上只能做「辅助问答」，无法做「任务执行」。

## 护城河

做车载 Agent 的壁垒不在 LLM 本身，而在：
1. **工具层的工程化**：把车载感知、规划、控制暴露为稳定、低延迟、可观测的 Tool
2. **安全层的构建**：确保 Agent 的每一次决策都经过确认
3. **场景数据的闭环**：Agent 的每一次失败都成为训练数据，持续提升成功率

这些恰好是智驾量产团队的核心能力，不是 AI Agent 新玩家短时间内能建立的。
